version: "3.8"

services: 
  server:
    build: .
    command: yarn start
    ports:
      - "3000:3000"
    depends_on: 
      - database
    environment:
      MONGO_HOST: database
      MONGO_PORT: 27017
  database:
    image: "mongo"
    ports:
      - "27017:27017"